info:
  name: get_receiving_po
  type: http
  seq: 5

http:
  method: GET
  url: http://{{host}}:{{port}}/api/inventory/receiving_report/purchase_order/17
  auth: inherit

runtime:
  variables:
    - name: port
      value: "3000"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
