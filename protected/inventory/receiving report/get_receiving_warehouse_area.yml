info:
  name: get_receiving_warehouse_area
  type: http
  seq: 8

http:
  method: GET
  url: http://{{host}}:{{port}}/api/inventory/receiving_report/warehouse_area/1
  auth: inherit

runtime:
  variables:
    - name: port
      value: "3000"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
