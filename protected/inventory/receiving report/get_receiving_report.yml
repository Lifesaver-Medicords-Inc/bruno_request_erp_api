info:
  name: get_receiving_report
  type: http
  seq: 6

http:
  method: GET
  url: http://{{host}}:{{port}}/api/inventory/receiving_report
  auth: inherit

runtime:
  variables:
    - name: port
      value: "3000"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
