info:
  name: clear_cache
  type: http
  seq: 1

http:
  method: GET
  url: http://{{host}}:{{port}}/api/admin/clear_all
  auth: inherit

runtime:
  variables:
    - name: port
      value: "3000"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
