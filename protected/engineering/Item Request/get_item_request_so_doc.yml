info:
  name: get_item_request_so_doc
  type: http
  seq: 4

http:
  method: GET
  url: http://{{host}}:{{port}}/api/engineering/item_request/sales_order_doc
  auth: inherit

runtime:
  variables:
    - name: port
      value: "3000"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
