info:
  name: get_item_request_users
  type: http
  seq: 3

http:
  method: GET
  url: http://{{host}}:{{port}}/api/engineering/item_request/users
  auth: inherit

runtime:
  variables:
    - name: port
      value: "3000"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
