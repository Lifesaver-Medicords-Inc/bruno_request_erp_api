info:
  name: get_item_request_so
  type: http
  seq: 5

http:
  method: GET
  url: http://{{host}}:{{port}}/api/engineering/item_request/sales_order/50030
  auth: inherit

runtime:
  variables:
    - name: port
      value: "3000"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
