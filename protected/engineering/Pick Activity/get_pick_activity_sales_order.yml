info:
  name: get_pick_activity_sales_order
  type: http
  seq: 5

http:
  method: GET
  url: http://{{host}}:{{port}}/api/engineering/pick_activity/sales_order/50030
  auth: inherit

runtime:
  variables:
    - name: port
      value: "3000"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
