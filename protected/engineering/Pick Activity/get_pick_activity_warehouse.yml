info:
  name: get_pick_activity_warehouse
  type: http
  seq: 2

http:
  method: GET
  url: http://{{host}}:{{port}}/api/engineering/pick_activity/warehouse
  auth: inherit

runtime:
  variables:
    - name: port
      value: "3000"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
