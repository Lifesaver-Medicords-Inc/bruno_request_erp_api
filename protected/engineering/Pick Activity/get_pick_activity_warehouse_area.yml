info:
  name: get_pick_activity_warehouse_area
  type: http
  seq: 3

http:
  method: GET
  url: http://{{host}}:{{port}}/api/engineering/pick_activity/warehouse_area/1
  auth: inherit

runtime:
  variables:
    - name: port
      value: "3000"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
